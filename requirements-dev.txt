# Everything needed to develop, build, and test. CI's test job installs this.
#
# Pulls in the runtime set transitively via both files below.
-r requirements-ingest.txt
-r requirements-lint.txt

pytest==9.1.1

# `pip install ./core` uses build isolation and reads these from
# core/pyproject.toml, so they are not strictly required here. They are listed
# so an editable/no-isolation build (`pip install --no-build-isolation
# -e ./core`) works too — which is how you iterate on the C++ core locally.
pybind11>=2.12
setuptools>=68
